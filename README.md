# key-system
In workspace, a folder named "Zones" inside two folders one named "1" and other "2" with each both having models inside them with the same named and the key meshes. Client is in startercharacterscripts, Main is in serverscripts and inside replicatedstorage theres a folder named modules and inside a module script named Config
